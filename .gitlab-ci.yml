image: ripsawridge/ruby-rsync:latest

stages:
  - test
  - build
  - deploy

variables:
  POSTGRES_DB: test_db
  POSTGRES_USER: runner
  POSTGRES_PASSWORD: ""

.setup_postgres: &setup_postgres
  services:
    - postgres:latest
  before_script:
    - apt-get update -qq && apt-get install -y -qq postgresql libpq-dev cmake

.setup_test_env: &setup_test_env
  <<: *setup_postgres
  before_script:
    - cp config/database.gitlab.yml config/database.yml
    - bundle install --path=vendor/bundler
    - RAILS_ENV=test bundle exec rake db:migrate

cache:
  untracked: true
  key: "$CI_COMMIT_REF_NAME"
  paths:
   - vendor/bundler
   - public

rspec:
  <<: *setup_test_env
  stage: test
  environment:
    name: test
  script:
    - RAILS_ENV=test bundle exec rspec

build:
  <<: *setup_postgres
  stage: build
  script:
  - bundle install --path=vendor/bundler
  - bundle exec rails server -e static --daemon
  - bundle exec rake static:generate

pages:
  stage: deploy
  cache:
    key: "$CI_COMMIT_REF_NAME"
    paths:
    - public
    policy: pull
  environment:
    name: static
    url: https://www.jpalmieri.com
  script: echo "deploying site to pages..."
  artifacts:
    paths:
    - public
  only:
  - master
